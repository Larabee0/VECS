<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <ControlTemplate x:Key="Vector2FieldTemplate"  TargetType="{x:Type VECS.UI.Vector2Field}">
    <Grid Width="580" Height="50" MinWidth="580" Background="#004D5052">
      <Grid.ColumnDefinitions>
        <ColumnDefinition/>
      </Grid.ColumnDefinitions>
      <Rectangle Grid.ColumnSpan="1" Fill="#FF262E35" Stroke="#FF000000"/>
      <Grid Margin="4,4,4,4" ColumnSpan="1">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="245" MaxWidth="245"/>
          <ColumnDefinition Width="15" MaxWidth="15"/>
          <ColumnDefinition Width="95" MaxWidth="95"/>
          <ColumnDefinition Width="15" MaxWidth="15"/>
          <ColumnDefinition Width="95" MaxWidth="95"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="Label" Text="{Binding Label, RelativeSource={RelativeSource TemplatedParent}}" HorizontalAlignment="Left" Foreground="#FFFFFFFF" Style="{DynamicResource VectorLabel}"/>
        <TextBlock Text="X" Grid.Column="1" TextWrapping="Wrap" Style="{DynamicResource VectorLabel}"/>
        <TextBlock Text="Y" Grid.Column="3" TextWrapping="Wrap" Style="{DynamicResource VectorLabel}"/>
        <TextBox x:Name="XComp" Text="{Binding ValueX, RelativeSource={RelativeSource TemplatedParent}}" Margin="0,0,2,0" Grid.Column="2" HorizontalAlignment="Left" Style="{DynamicResource VectorStyle}"/>
        <TextBox x:Name="YComp" Text="{Binding ValueY, RelativeSource={RelativeSource TemplatedParent}}" Margin="2,0,2,0" Grid.Column="4" HorizontalAlignment="Left" TextWrapping="Wrap" Style="{DynamicResource VectorStyle}"/>
      </Grid>
    </Grid>
  </ControlTemplate>
  <Style x:Key="VECS.UI.Vector2Field"  TargetType="{x:Type VECS.UI.Vector2Field}">
    <Setter Property="Template" Value="{StaticResource Vector2FieldTemplate}"/>
  </Style>
</ResourceDictionary>